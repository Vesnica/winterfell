[workspace]
members = [
  "utils/core",
  "utils/rand",
  "math",
  "crypto",
  "fri",
  "air",
  "prover",
  "verifier",
  "winterfell",
  "examples"
]

[profile.release]
codegen-units = 1
lto = true
strip = "debuginfo"

[profile.bench]
codegen-units = 1
lto = true
